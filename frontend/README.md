## Uruchomienie

1. Backend: w folderze `backend_planeat/` uruchom serwer (wymaga MySQL i pliku `.env` z danymi połączenia):

```bash
cd ../backend_planeat
npm install
node src/server.js
```

2. Frontend: w folderze `frontend_planeat/`:

```bash
cd ../frontend_planeat
npm install
npm run dev
```
